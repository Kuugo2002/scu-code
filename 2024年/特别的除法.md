# 特别的除法

## 题目描述

输入正整数 `a`, `b`, `c`，输出 `a/b` 的小数形式，精确到小数点后 `c` 位。

- **数据范围**：
  - `a`, `b` 的范围都是 `1 ~ 1e8`
  - `c` 的范围是 `2 ~ 1000`

请计算 `a/b`，保留 `c` 位小数。

## 输入样例

```
2 3 6
```

## 输出样例

```
0.666667
```